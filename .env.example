# ASECO Values & Culture - Environment Variables
# Copy to .env and .env.production for deployment

# Optional: API base URL (if backend is added later)
# REACT_APP_API_URL=https://api.example.com

# Optional: Public URL for subpath deployment (e.g. /aseco)
# PUBLIC_URL=/aseco
